//
// This module is for logging. Currently there is a very simple log function,
// but it is meant to be easier to change than if `console.log` was used
// everywhere.
//

/**
 * Logs the message `message` to the console.
 */
function log(message: string) {
    console.log(message);
}

export { log };
